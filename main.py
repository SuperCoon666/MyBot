# from brain import dp
# from brain import types
#
#
# @dp.message_handler(lambda message: message.text == "Friend")
# async def with_puree(message: types.Message):
#     await message.reply("Отличный выбор!")
